# Copier ce fichier en `.env.local` (non versionné) et renseigner les valeurs.
# Les variables exposées au navigateur doivent être préfixées par NEXT_PUBLIC_.

# NEXT_PUBLIC_SITE_URL=http://localhost:3000
