# Copier ce fichier en `.env.local` (non versionné) et renseigner les valeurs.
# Sur Vercel, les déclarer dans Project Settings → Environment Variables.

# URL publique du site. Sert aux métadonnées absolues (Open Graph), au sitemap
# et à robots.txt.
#
# Facultative sur Vercel : à défaut, le domaine de production du projet est
# utilisé automatiquement, puis l'URL du déploiement courant pour les previews,
# et enfin http://localhost:3000 en local. Une variable déclarée mais vide est
# traitée comme absente, et le protocole est ajouté s'il manque.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Endpoint d'inscription à la newsletter (Brevo, Mailchimp, Klaviyo, Resend
# Audiences, webhook maison…). Reçoit un POST JSON `{ "email": "…" }`.
# Tant qu'elle est absente, /api/newsletter répond 503 et le formulaire affiche
# un message d'indisponibilité — plutôt que de perdre des adresses en silence.
# NEWSLETTER_WEBHOOK_URL=
